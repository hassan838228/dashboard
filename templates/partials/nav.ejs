<nav class="nav" role="navigation">
  <div class="nav-container">
    <!-- Logo -->
    <div class="logo">
      <a href="<%=domain%>" rel="noopener" aria-label="Open home page" class="name">
        <img src="<%= domain %>/logo.png" alt="Cbot Logo" class="logo-img">
        <span class="logo-text">Cbot</span>
      </a>
    </div>
    
    <!-- Desktop Menu -->
    <ul class="menu desktop-menu">
      <% if (user) { %>
        <li><a href="<%=domain%>/server" target="_blank" rel="noopener" class="nav-item">
          <i class="fas fa-headset"></i>
          <span>Support</span>
        </a></li>
        <li><a href="<%=domain%>/commands" target="_blank" rel="noopener" class="nav-item">
          <i class="fas fa-terminal"></i>
          <span>Commands</span>
        </a></li>
        <li><a href="<%=domain%>/premium" class="nav-item premium-link">
          <i class="fas fa-crown"></i>
          <span>Premium</span>
        </a></li>
        
        <!-- User Avatar and Dropdown -->
        <% if (user.avatar) { 
          avatar = "https://cdn.discordapp.com/avatars/" + user.id + "/" + user.avatar + ".png?size=128"; 
        } else { 
          avatar = "https://cdn.glitch.com/82fe990a-7942-42e3-9790-39807ccdb9f6%2Ficon-404.png?v=1602412158188"; 
        } %>
        <li class="user-section">
          <div class="avatar-container" id="userDropdownToggle" onclick="toggleUserDropdown()">
            <img class="user-avatar" src="<%= avatar %>" alt="User Avatar">
            <div class="user-info">
              <span class="user-name"><%= user.username %></span>
              <span class="user-discriminator">#<%= user.discriminator %></span>
            </div>
            <i class="fas fa-chevron-down dropdown-arrow"></i>
          </div>
        </li>
      <% } else { %>
        <li><a href="<%=domain%>/stats" target="_blank" rel="noopener" class="nav-item">
          <i class="fas fa-chart-bar"></i>
          <span>Stats</span>
        </a></li>
        <li><a href="<%=domain%>/support" target="_blank" rel="noopener" class="nav-item">
          <i class="fas fa-headset"></i>
          <span>Support</span>
        </a></li>
        <li><a href="<%=domain%>/commands" target="_blank" rel="noopener" class="nav-item">
          <i class="fas fa-terminal"></i>
          <span>Commands</span>
        </a></li>
        <li><a href="<%=domain%>/premium" class="nav-item premium-link">
          <i class="fas fa-crown"></i>
          <span>Premium</span>
        </a></li>
        <li><a href="<%=domain%>/login" class="nav-item login-btn">
          <i class="fab fa-discord"></i>
          <span>Login</span>
        </a></li>
      <% } %>
    </ul>

    <!-- Mobile Toggle -->
    <div class="mobile-toggle" id="mobileToggle" onclick="toggleMobileMenu()">
      <div class="toggle-menu">
        <div class="line line1"></div>
        <div class="line line2"></div>
        <div class="line line3"></div>
      </div>
    </div>
  </div>

  <!-- Mobile Menu -->
  <div class="mobile-menu" id="mobileMenu">
    <div class="mobile-menu-content">
      <% if (user) { %>
        <div class="mobile-user-info">
          <img class="mobile-user-avatar" src="<%= avatar %>" alt="User Avatar">
          <div class="mobile-user-details">
            <span class="mobile-user-name"><%= user.username %></span>
            <span class="mobile-user-discriminator">#<%= user.discriminator %></span>
          </div>
        </div>
        <div class="mobile-nav-divider"></div>
      <% } %>
      
      <ul class="mobile-nav-list">
        <li class="mobile-nav-item">
          <a href="<%=domain%>/" class="mobile-nav-link">
            <i class="fas fa-home"></i>
            <span>Home</span>
          </a>
        </li>
        
        <% if (user) { %>
          <li class="mobile-nav-item">
            <a href="<%=domain%>/dashboard" class="mobile-nav-link">
              <i class="fas fa-tachometer-alt"></i>
              <span>Dashboard</span>
            </a>
          </li>
        <% } %>
        
        <li class="mobile-nav-item">
          <a href="<%=domain%>/stats" class="mobile-nav-link">
            <i class="fas fa-chart-bar"></i>
            <span>Stats</span>
          </a>
        </li>
        <li class="mobile-nav-item">
          <a href="<%=domain%>/support" class="mobile-nav-link">
            <i class="fas fa-headset"></i>
            <span>Support</span>
          </a>
        </li>
        <li class="mobile-nav-item">
          <a href="<%=domain%>/commands" class="mobile-nav-link">
            <i class="fas fa-terminal"></i>
            <span>Commands</span>
          </a>
        </li>
        <li class="mobile-nav-item">
          <a href="<%=domain%>/premium" class="mobile-nav-link premium-mobile">
            <i class="fas fa-crown"></i>
            <span>Premium</span>
          </a>
        </li>
        
        <% if (user) { %>
          <div class="mobile-nav-divider"></div>
          <li class="mobile-nav-item">
            <a href="<%=domain%>/logout" class="mobile-nav-link logout-mobile">
              <i class="fas fa-sign-out-alt"></i>
              <span>Logout</span>
            </a>
          </li>
        <% } else { %>
          <div class="mobile-nav-divider"></div>
          <li class="mobile-nav-item">
            <a href="<%=domain%>/login" class="mobile-nav-link login-mobile">
              <i class="fab fa-discord"></i>
              <span>Login with Discord</span>
            </a>
          </li>
        <% } %>
      </ul>
    </div>
  </div>

  <!-- User Dropdown (Desktop) -->
  <% if (user) { %>
  <div class="dropdown user-dropdown" id="userDropdown">
    <div class="dropdown-header">
      <img class="dropdown-avatar" src="<%= avatar %>" alt="User Avatar">
      <div class="dropdown-user-info">
        <span class="dropdown-username"><%= user.username %></span>
        <span class="dropdown-discriminator">#<%= user.discriminator %></span>
      </div>
    </div>
    
    <div class="dropdown-divider"></div>
    
    <div class="dropdown-content">
      <% if (path === "/dashboard") { %>
        <a class="dropdown-li" href="<%=domain%>/">
          <i class="fas fa-home"></i>
          <span>Home</span>
        </a>
      <% } else if (path !== "/") { %>
        <a class="dropdown-li" href="<%=domain%>/">
          <i class="fas fa-home"></i>
          <span>Home</span>
        </a>
        <a class="dropdown-li" href="<%=domain%>/dashboard">
          <i class="fas fa-tachometer-alt"></i>
          <span>My Servers</span>
        </a>
      <% } else { %>
        <a class="dropdown-li" href="<%=domain%>/dashboard">
          <i class="fas fa-tachometer-alt"></i>
          <span>My Servers</span>
        </a>
      <% } %>
      
      <a class="dropdown-li" href="<%=domain%>/premium">
        <i class="fas fa-crown"></i>
        <span>Premium</span>
      </a>
      
      <div class="dropdown-divider"></div>
      
      <a class="dropdown-li logout" href="<%=domain%>/logout">
        <i class="fas fa-sign-out-alt"></i>
        <span>Logout</span>
      </a>
    </div>
  </div>
  <% } %>
</nav>

<!-- Navigation Styles -->
<style>
  /* Navigation Styles */
  .nav {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    background: rgba(26, 26, 26, 0.95);
    backdrop-filter: blur(10px);
    border-bottom: 1px solid rgba(255, 215, 0, 0.1);
    z-index: 9999;
    transition: all 0.3s ease;
  }

  .nav-container {
    display: flex;
    align-items: center;
    justify-content: space-between;
    max-width: 1400px;
    margin: 0 auto;
    padding: 0 2rem;
    height: 80px;
  }

  /* Logo */
  .logo .name {
    display: flex;
    align-items: center;
    text-decoration: none;
    gap: 0.75rem;
    transition: all 0.3s ease;
  }

  .logo .name:hover {
    transform: translateY(-2px);
  }

  .logo-img {
    width: 45px;
    height: 45px;
    border-radius: 50%;
    border: 2px solid var(--gold-primary);
    filter: drop-shadow(0 0 10px rgba(255, 215, 0, 0.3));
    transition: all 0.3s ease;
  }

  .logo .name:hover .logo-img {
    transform: rotate(5deg) scale(1.05);
    box-shadow: 0 0 20px rgba(255, 215, 0, 0.5);
  }

  .logo-text {
    font-size: 1.8rem;
    font-weight: 700;
    background: linear-gradient(45deg, var(--gold-primary), var(--gold-secondary));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  /* Desktop Menu */
  .desktop-menu {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .nav-item {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.75rem 1.25rem;
    text-decoration: none;
    color: var(--text-secondary);
    border-radius: 10px;
    transition: all 0.3s ease;
    font-weight: 500;
    position: relative;
    overflow: hidden;
  }

  .nav-item::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255, 215, 0, 0.1), transparent);
    transition: all 0.3s ease;
  }

  .nav-item:hover::before {
    left: 100%;
  }

  .nav-item:hover {
    color: var(--gold-primary);
    background: rgba(255, 215, 0, 0.1);
    transform: translateY(-2px);
  }

  .nav-item i {
    font-size: 1rem;
    transition: all 0.3s ease;
  }

  .nav-item:hover i {
    transform: scale(1.1);
  }

  .premium-link {
    background: linear-gradient(45deg, var(--gold-primary), var(--gold-secondary));
    color: var(--bg-primary) !important;
    font-weight: 600;
    box-shadow: 0 4px 15px rgba(255, 215, 0, 0.3);
  }

  .premium-link:hover {
    background: linear-gradient(45deg, var(--gold-secondary), var(--gold-accent));
    box-shadow: 0 6px 20px rgba(255, 215, 0, 0.4);
    transform: translateY(-3px);
  }

  .login-btn {
    background: linear-gradient(45deg, #7289da, #5865f2);
    color: white !important;
    font-weight: 600;
  }

  .login-btn:hover {
    background: linear-gradient(45deg, #5865f2, #4752c4);
    transform: translateY(-2px);
  }

  /* User Section */
  .user-section {
    position: relative;
  }

  .avatar-container {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    padding: 0.5rem 1rem;
    background: rgba(255, 215, 0, 0.1);
    border: 1px solid rgba(255, 215, 0, 0.3);
    border-radius: 12px;
    cursor: pointer;
    transition: all 0.3s ease;
  }

  .avatar-container:hover {
    background: rgba(255, 215, 0, 0.2);
    border-color: var(--gold-primary);
    transform: translateY(-2px);
    box-shadow: 0 8px 25px rgba(255, 215, 0, 0.3);
  }

  .user-avatar {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    border: 2px solid var(--gold-primary);
    transition: all 0.3s ease;
  }

  .avatar-container:hover .user-avatar {
    border-color: var(--gold-secondary);
    transform: scale(1.05);
  }

  .user-info {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
  }

  .user-name {
    font-weight: 600;
    color: var(--text-primary);
    font-size: 0.95rem;
  }

  .user-discriminator {
    font-size: 0.8rem;
    color: var(--text-muted);
  }

  .dropdown-arrow {
    font-size: 0.8rem;
    color: var(--gold-primary);
    transition: all 0.3s ease;
  }

  .avatar-container:hover .dropdown-arrow {
    transform: translateY(2px);
  }

  /* User Dropdown */
  .user-dropdown {
    position: absolute;
    top: calc(100% + 10px);
    right: 0;
    background: var(--bg-card);
    border: 1px solid rgba(255, 215, 0, 0.2);
    border-radius: 12px;
    min-width: 250px;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
    opacity: 0;
    visibility: hidden;
    transform: translateY(-10px);
    transition: all 0.3s ease;
  }

  .user-dropdown.show {
    opacity: 1;
    visibility: visible;
    transform: translateY(0);
  }

  .dropdown-header {
    display: flex;
    align-items: center;
    gap: 1rem;
    padding: 1.5rem;
    border-bottom: 1px solid rgba(255, 215, 0, 0.1);
  }

  .dropdown-avatar {
    width: 50px;
    height: 50px;
    border-radius: 50%;
    border: 2px solid var(--gold-primary);
  }

  .dropdown-user-info {
    display: flex;
    flex-direction: column;
  }

  .dropdown-username {
    font-weight: 600;
    color: var(--text-primary);
    font-size: 1.1rem;
  }

  .dropdown-discriminator {
    color: var(--text-muted);
    font-size: 0.9rem;
  }

  .dropdown-content {
    padding: 0.5rem;
  }

  .dropdown-li {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    padding: 0.75rem 1rem;
    color: var(--text-secondary);
    text-decoration: none;
    border-radius: 8px;
    transition: all 0.3s ease;
    margin: 0.25rem 0;
  }

  .dropdown-li:hover {
    background: rgba(255, 215, 0, 0.1);
    color: var(--gold-primary);
    transform: translateX(5px);
  }

  .dropdown-li i {
    font-size: 1rem;
    width: 20px;
    text-align: center;
  }

  .dropdown-divider {
    height: 1px;
    background: rgba(255, 215, 0, 0.1);
    margin: 0.5rem 0;
  }

  .logout {
    color: #f04747 !important;
  }

  .logout:hover {
    background: rgba(240, 71, 71, 0.1) !important;
    color: #f04747 !important;
  }

  /* Mobile Toggle */
  .mobile-toggle {
    display: none;
    cursor: pointer;
  }

  .toggle-menu {
    width: 30px;
    height: 25px;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    align-items: center;
  }

  .line {
    width: 100%;
    height: 3px;
    background: var(--gold-primary);
    border-radius: 3px;
    transition: all 0.3s ease;
  }

  .mobile-toggle.active .line1 {
    transform: rotate(45deg) translate(7px, 7px);
  }

  .mobile-toggle.active .line2 {
    opacity: 0;
  }

  .mobile-toggle.active .line3 {
    transform: rotate(-45deg) translate(7px, -7px);
  }

  /* Mobile Menu */
  .mobile-menu {
    position: fixed;
    top: 80px;
    left: 0;
    right: 0;
    background: var(--bg-primary);
    backdrop-filter: blur(10px);
    border-top: 1px solid rgba(255, 215, 0, 0.1);
    max-height: 0;
    overflow: hidden;
    transition: all 0.3s ease;
  }

  .mobile-menu.show {
    max-height: calc(100vh - 80px);
  }

  .mobile-menu-content {
    padding: 2rem;
  }

  .mobile-user-info {
    display: flex;
    align-items: center;
    gap: 1rem;
    margin-bottom: 1rem;
  }

  .mobile-user-avatar {
    width: 50px;
    height: 50px;
    border-radius: 50%;
    border: 2px solid var(--gold-primary);
  }

  .mobile-user-details {
    display: flex;
    flex-direction: column;
  }

  .mobile-user-name {
    font-weight: 600;
    color: var(--text-primary);
    font-size: 1.1rem;
  }

  .mobile-user-discriminator {
    color: var(--text-muted);
    font-size: 0.9rem;
  }

  .mobile-nav-divider {
    height: 1px;
    background: rgba(255, 215, 0, 0.1);
    margin: 1rem 0;
  }

  .mobile-nav-list {
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .mobile-nav-item {
    margin: 0.5rem 0;
  }

  .mobile-nav-link {
    display: flex;
    align-items: center;
    gap: 1rem;
    padding: 1rem;
    color: var(--text-secondary);
    text-decoration: none;
    border-radius: 12px;
    transition: all 0.3s ease;
    font-weight: 500;
  }

  .mobile-nav-link:hover {
    background: rgba(255, 215, 0, 0.1);
    color: var(--gold-primary);
    transform: translateX(10px);
  }

  .mobile-nav-link i {
    font-size: 1.2rem;
    width: 25px;
    text-align: center;
  }

  .premium-mobile {
    background: linear-gradient(45deg, var(--gold-primary), var(--gold-secondary));
    color: var(--bg-primary) !important;
    font-weight: 600;
  }

  .premium-mobile:hover {
    background: linear-gradient(45deg, var(--gold-secondary), var(--gold-accent));
    transform: translateX(15px);
  }

  .login-mobile {
    background: linear-gradient(45deg, #7289da, #5865f2);
    color: white !important;
    font-weight: 600;
  }

  .login-mobile:hover {
    background: linear-gradient(45deg, #5865f2, #4752c4);
    transform: translateX(15px);
  }

  .logout-mobile {
    background: rgba(240, 71, 71, 0.1);
    color: #f04747 !important;
  }

  .logout-mobile:hover {
    background: rgba(240, 71, 71, 0.2);
    color: #f04747 !important;
    transform: translateX(15px);
  }

  /* Responsive Design */
  @media (max-width: 768px) {
    .desktop-menu {
      display: none;
    }
    
    .mobile-toggle {
      display: block;
    }

    .nav-container {
      padding: 0 1rem;
    }

    .logo-text {
      font-size: 1.5rem;
    }

    .logo-img {
      width: 35px;
      height: 35px;
    }
  }

  @media (max-width: 480px) {
    .nav-container {
      padding: 0 0.5rem;
    }

    .mobile-menu-content {
      padding: 1rem;
    }
  }
</style>

<!-- Navigation JavaScript -->
<script>
  // Toggle user dropdown
  function toggleUserDropdown() {
    const dropdown = document.getElementById('userDropdown');
    if (dropdown) {
      dropdown.classList.toggle('show');
    }
  }

  // Toggle mobile menu
  function toggleMobileMenu() {
    const mobileMenu = document.getElementById('mobileMenu');
    const mobileToggle = document.getElementById('mobileToggle');
    
    if (mobileMenu && mobileToggle) {
      mobileMenu.classList.toggle('show');
      mobileToggle.classList.toggle('active');
    }
  }

  // Close dropdowns when clicking outside
  document.addEventListener('click', function(event) {
    const userDropdown = document.getElementById('userDropdown');
    const userDropdownToggle = document.getElementById('userDropdownToggle');
    const mobileMenu = document.getElementById('mobileMenu');
    const mobileToggle = document.getElementById('mobileToggle');

    // Close user dropdown if clicking outside
    if (userDropdown && !userDropdownToggle.contains(event.target)) {
      userDropdown.classList.remove('show');
    }

    // Close mobile menu if clicking outside
    if (mobileMenu && !mobileToggle.contains(event.target) && !mobileMenu.contains(event.target)) {
      mobileMenu.classList.remove('show');
      mobileToggle.classList.remove('active');
    }
  });

  // Handle navbar scroll effect
  window.addEventListener('scroll', function() {
    const nav = document.querySelector('.nav');
    if (nav) {
      if (window.scrollY > 50) {
        nav.style.background = 'rgba(26, 26, 26, 0.98)';
        nav.style.backdropFilter = 'blur(15px)';
      } else {
        nav.style.background = 'rgba(26, 26, 26, 0.95)';
        nav.style.backdropFilter = 'blur(10px)';
      }
    }
  });

  // Add Font Awesome if not already loaded
  if (!document.querySelector('link[href*="font-awesome"]')) {
    const fontAwesome = document.createElement('link');
    fontAwesome.rel = 'stylesheet';
    fontAwesome.href = 'https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css';
    document.head.appendChild(fontAwesome);
  }
</script>
