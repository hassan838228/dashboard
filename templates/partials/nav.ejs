<nav class="modern-nav" role="navigation">
    <div class="nav-container">
        <div class="nav-brand">
            <a href="<%=domain%>" rel="noopener" aria-label="Open home page">
                <img src="<%= domain %>/logo.png" alt="Cbot Logo" class="brand-logo">
                <span class="brand-text">Cbot</span>
            </a>
        </div>

        <!-- Desktop Navigation -->
        <div class="nav-desktop">
            <ul class="nav-links">
                <li><a href="<%=domain%>/" class="nav-link <% if (path === '/') { %>active<% } %>">
                    <i class="fas fa-home"></i>
                    Home
                </a></li>
                <li><a href="<%=domain%>/stats" class="nav-link">
                    <i class="fas fa-chart-bar"></i>
                    Stats
                </a></li>
                <li><a href="<%=domain%>/commands" class="nav-link">
                    <i class="fas fa-terminal"></i>
                    Commands
                </a></li>
                <li><a href="<%=domain%>/support" class="nav-link">
                    <i class="fas fa-life-ring"></i>
                    Support
                </a></li>
            </ul>

            <div class="nav-actions">
                <% if (user) { %>
                    <!-- User Profile Section -->
                    <div class="user-profile" id="userProfile">
                        <% 
                        let avatar;
                        if (user.avatar) { 
                            avatar = "https://cdn.discordapp.com/avatars/" + user.id + "/" + user.avatar + ".png?size=128"; 
                        } else { 
                            avatar = "https://cdn.glitch.com/82fe990a-7942-42e3-9790-39807ccdb9f6%2Ficon-404.png?v=1602412158188"; 
                        } 
                        %>
                        
                        <!-- Language Selector -->
                        <div class="language-selector">
                            <button class="lang-btn" id="langBtn">
                                <i class="fas fa-globe"></i>
                                <i class="fas fa-chevron-down"></i>
                            </button>
                            <div class="lang-dropdown" id="langDropdown">
                                <a href="#" class="lang-option" data-lang="en">
                                    <span class="flag">🇺🇸</span>
                                    English
                                </a>
                                <a href="#" class="lang-option" data-lang="ar">
                                    <span class="flag">🇸🇦</span>
                                    العربية
                                </a>
                            </div>
                        </div>

                        <!-- User Avatar and Dropdown -->
                        <div class="profile-dropdown">
                            <button class="profile-btn" id="profileBtn">
                                <img src="<%= avatar %>" alt="User Avatar" class="user-avatar">
                                <div class="user-info">
                                    <span class="username"><%= user.username %></span>
                                    <span class="discriminator">#<%= user.discriminator %></span>
                                </div>
                                <i class="fas fa-chevron-down"></i>
                            </button>

                            <div class="profile-dropdown-menu" id="profileDropdown">
                                <div class="dropdown-header">
                                    <img src="<%= avatar %>" alt="User Avatar" class="dropdown-avatar">
                                    <div class="dropdown-user-info">
                                        <span class="dropdown-username"><%= user.username %></span>
                                        <span class="dropdown-discriminator">#<%= user.discriminator %></span>
                                    </div>
                                </div>
                                
                                <div class="dropdown-divider"></div>
                                
                                <div class="dropdown-content">
                                    <% if (path !== "/dashboard") { %>
                                        <a href="<%=domain%>/dashboard" class="dropdown-item">
                                            <i class="fas fa-tachometer-alt"></i>
                                            <span>My Servers</span>
                                        </a>
                                    <% } else { %>
                                        <a href="<%=domain%>/" class="dropdown-item">
                                            <i class="fas fa-home"></i>
                                            <span>Home</span>
                                        </a>
                                    <% } %>
                                    
                                    <a href="<%=domain%>/premium" class="dropdown-item premium">
                                        <i class="fas fa-crown"></i>
                                        <span>Premium</span>
                                        <span class="premium-badge">Pro</span>
                                    </a>
                                    
                                    <a href="<%=domain%>/status" class="dropdown-item">
                                        <i class="fas fa-heartbeat"></i>
                                        <span>Bot Status</span>
                                        <span class="status-indicator online"></span>
                                    </a>
                                    
                                    <div class="dropdown-divider"></div>
                                    
                                    <a href="<%=domain%>/logout" class="dropdown-item logout">
                                        <i class="fas fa-sign-out-alt"></i>
                                        <span>Logout</span>
                                    </a>
                                </div>
                            </div>
                        </div>
                    <% } else { %>
                        <!-- Language Selector for guests -->
                        <div class="language-selector">
                            <button class="lang-btn" id="langBtn">
                                <i class="fas fa-globe"></i>
                                <i class="fas fa-chevron-down"></i>
                            </button>
                            <div class="lang-dropdown" id="langDropdown">
                                <a href="#" class="lang-option" data-lang="en">
                                    <span class="flag">🇺🇸</span>
                                    English
                                </a>
                                <a href="#" class="lang-option" data-lang="ar">
                                    <span class="flag">🇸🇦</span>
                                    العربية
                                </a>
                            </div>
                        </div>

                        <!-- Login Button -->
                        <a href="<%=domain%>/login" class="login-btn">
                            <i class="fab fa-discord"></i>
                            Login with Discord
                        </a>
                    <% } %>
                </div>
            </div>
        </div>

        <!-- Mobile Navigation -->
        <div class="nav-mobile">
            <div class="mobile-actions">
                <!-- Language Selector Mobile -->
                <div class="language-selector-mobile">
                    <button class="lang-btn-mobile" id="langBtnMobile">
                        <i class="fas fa-globe"></i>
                    </button>
                </div>

                <!-- Mobile Menu Toggle -->
                <button class="mobile-menu-toggle" id="mobileMenuToggle">
                    <span></span>
                    <span></span>
                    <span></span>
                </button>
            </div>

            <!-- Mobile Menu -->
            <div class="mobile-menu" id="mobileMenu">
                <div class="mobile-menu-content">
                    <% if (user) { %>
                        <!-- Mobile User Profile -->
                        <div class="mobile-profile">
                            <img src="<%= avatar %>" alt="User Avatar" class="mobile-avatar">
                            <div class="mobile-user-info">
                                <span class="mobile-username"><%= user.username %></span>
                                <span class="mobile-discriminator">#<%= user.discriminator %></span>
                            </div>
                        </div>
                        <div class="mobile-divider"></div>
                    <% } %>

                    <!-- Mobile Navigation Links -->
                    <div class="mobile-nav-links">
                        <a href="<%=domain%>/" class="mobile-nav-link">
                            <i class="fas fa-home"></i>
                            <span>Home</span>
                        </a>
                        
                        <% if (user) { %>
                            <a href="<%=domain%>/dashboard" class="mobile-nav-link">
                                <i class="fas fa-tachometer-alt"></i>
                                <span>My Servers</span>
                            </a>
                        <% } %>
                        
                        <a href="<%=domain%>/stats" class="mobile-nav-link">
                            <i class="fas fa-chart-bar"></i>
                            <span>Stats</span>
                        </a>
                        
                        <a href="<%=domain%>/commands" class="mobile-nav-link">
                            <i class="fas fa-terminal"></i>
                            <span>Commands</span>
                        </a>
                        
                        <a href="<%=domain%>/support" class="mobile-nav-link">
                            <i class="fas fa-life-ring"></i>
                            <span>Support</span>
                        </a>

                        <% if (user) { %>
                            <a href="<%=domain%>/premium" class="mobile-nav-link premium">
                                <i class="fas fa-crown"></i>
                                <span>Premium</span>
                            </a>
                            
                            <a href="<%=domain%>/status" class="mobile-nav-link">
                                <i class="fas fa-heartbeat"></i>
                                <span>Bot Status</span>
                            </a>
                            
                            <div class="mobile-divider"></div>
                            
                            <a href="<%=domain%>/logout" class="mobile-nav-link logout">
                                <i class="fas fa-sign-out-alt"></i>
                                <span>Logout</span>
                            </a>
                        <% } else { %>
                            <div class="mobile-divider"></div>
                            <a href="<%=domain%>/login" class="mobile-nav-link login">
                                <i class="fab fa-discord"></i>
                                <span>Login with Discord</span>
                            </a>
                        <% } %>
                    </div>
                </div>
            </div>
        </div>
    </div>
</nav>

<style>
/* Modern Navigation Styles */
.modern-nav {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    background: rgba(26, 31, 53, 0.95);
    backdrop-filter: blur(20px);
    border-bottom: 1px solid rgba(44, 50, 68, 0.5);
    z-index: 1000;
    transition: all 0.3s ease;
}

.nav-container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 24px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    height: 70px;
}

/* Brand */
.nav-brand a {
    display: flex;
    align-items: center;
    gap: 12px;
    text-decoration: none;
    transition: all 0.3s ease;
}

.nav-brand a:hover {
    transform: scale(1.05);
}

.brand-logo {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    box-shadow: 0 4px 16px rgba(88, 101, 242, 0.3);
}

.brand-text {
    font-size: 1.5rem;
    font-weight: 700;
    background: linear-gradient(135deg, #5865f2 0%, #00d4aa 100%);
    background-clip: text;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
}

/* Desktop Navigation */
.nav-desktop {
    display: flex;
    align-items: center;
    gap: 32px;
}

.nav-links {
    display: flex;
    list-style: none;
    margin: 0;
    padding: 0;
    gap: 24px;
}

.nav-link {
    display: flex;
    align-items: center;
    gap: 8px;
    padding: 8px 16px;
    border-radius: 8px;
    text-decoration: none;
    color: #b9bbbe;
    font-weight: 500;
    transition: all 0.3s ease;
    position: relative;
}

.nav-link:hover {
    color: #ffffff;
    background: rgba(88, 101, 242, 0.1);
}

.nav-link.active {
    color: #5865f2;
    background: rgba(88, 101, 242, 0.15);
}

.nav-link i {
    font-size: 14px;
}

.nav-actions {
    display: flex;
    align-items: center;
    gap: 16px;
}

/* Language Selector */
.language-selector {
    position: relative;
}

.lang-btn {
    display: flex;
    align-items: center;
    gap: 8px;
    padding: 8px 12px;
    background: rgba(30, 36, 57, 0.8);
    border: 1px solid rgba(44, 50, 68, 0.5);
    border-radius: 8px;
    color: #b9bbbe;
    cursor: pointer;
    transition: all 0.3s ease;
}

.lang-btn:hover {
    background: rgba(88, 101, 242, 0.1);
    border-color: rgba(88, 101, 242, 0.3);
    color: #ffffff;
}

.lang-dropdown {
    position: absolute;
    top: calc(100% + 8px);
    right: 0;
    background: rgba(30, 36, 57, 0.95);
    backdrop-filter: blur(20px);
    border: 1px solid rgba(44, 50, 68, 0.5);
    border-radius: 12px;
    padding: 8px;
    min-width: 140px;
    opacity: 0;
    visibility: hidden;
    transform: translateY(-10px);
    transition: all 0.3s ease;
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
}

.lang-dropdown.active {
    opacity: 1;
    visibility: visible;
    transform: translateY(0);
}

.lang-option {
    display: flex;
    align-items: center;
    gap: 12px;
    padding: 8px 12px;
    border-radius: 8px;
    text-decoration: none;
    color: #b9bbbe;
    font-size: 14px;
    transition: all 0.3s ease;
}

.lang-option:hover {
    background: rgba(88, 101, 242, 0.1);
    color: #ffffff;
}

.flag {
    font-size: 16px;
}

/* User Profile */
.user-profile {
    display: flex;
    align-items: center;
    gap: 16px;
}

.profile-dropdown {
    position: relative;
}

.profile-btn {
    display: flex;
    align-items: center;
    gap: 12px;
    padding: 6px;
    background: rgba(30, 36, 57, 0.8);
    border: 1px solid rgba(44, 50, 68, 0.5);
    border-radius: 12px;
    cursor: pointer;
    transition: all 0.3s ease;
}

.profile-btn:hover {
    background: rgba(88, 101, 242, 0.1);
    border-color: rgba(88, 101, 242, 0.3);
}

.user-avatar {
    width: 36px;
    height: 36px;
    border-radius: 50%;
    border: 2px solid rgba(88, 101, 242, 0.3);
}

.user-info {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
}

.username {
    font-size: 14px;
    font-weight: 600;
    color: #ffffff;
    line-height: 1;
}

.discriminator {
    font-size: 12px;
    color: #72767d;
    line-height: 1;
}

.profile-btn i {
    color: #72767d;
    font-size: 12px;
    transition: transform 0.3s ease;
}

.profile-btn.active i {
    transform: rotate(180deg);
}

/* Profile Dropdown Menu */
.profile-dropdown-menu {
    position: absolute;
    top: calc(100% + 8px);
    right: 0;
    background: rgba(30, 36, 57, 0.95);
    backdrop-filter: blur(20px);
    border: 1px solid rgba(44, 50, 68, 0.5);
    border-radius: 16px;
    padding: 16px;
    min-width: 280px;
    opacity: 0;
    visibility: hidden;
    transform: translateY(-10px);
    transition: all 0.3s ease;
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
}

.profile-dropdown-menu.active {
    opacity: 1;
    visibility: visible;
    transform: translateY(0);
}

.dropdown-header {
    display: flex;
    align-items: center;
    gap: 12px;
    margin-bottom: 16px;
}

.dropdown-avatar {
    width: 48px;
    height: 48px;
    border-radius: 50%;
    border: 2px solid rgba(88, 101, 242, 0.3);
}

.dropdown-user-info {
    display: flex;
    flex-direction: column;
}

.dropdown-username {
    font-size: 16px;
    font-weight: 600;
    color: #ffffff;
    line-height: 1.2;
}

.dropdown-discriminator {
    font-size: 14px;
    color: #72767d;
    line-height: 1.2;
}

.dropdown-divider {
    height: 1px;
    background: rgba(44, 50, 68, 0.5);
    margin: 12px 0;
}

.dropdown-content {
    display: flex;
    flex-direction: column;
    gap: 4px;
}

.dropdown-item {
    display: flex;
    align-items: center;
    gap: 12px;
    padding: 12px 16px;
    border-radius: 8px;
    text-decoration: none;
    color: #b9bbbe;
    font-size: 14px;
    font-weight: 500;
    transition: all 0.3s ease;
    position: relative;
}

.dropdown-item:hover {
    background: rgba(88, 101, 242, 0.1);
    color: #ffffff;
}

.dropdown-item i {
    width: 16px;
    text-align: center;
}

.dropdown-item.premium {
    color: #faa61a;
}

.dropdown-item.premium:hover {
    background: rgba(250, 166, 26, 0.1);
}

.dropdown-item.logout {
    color: #ed4245;
}

.dropdown-item.logout:hover {
    background: rgba(237, 66, 69, 0.1);
}

.premium-badge {
    background: linear-gradient(135deg, #faa61a 0%, #f39c12 100%);
    color: #ffffff;
    font-size: 10px;
    font-weight: 700;
    padding: 2px 6px;
    border-radius: 4px;
    margin-left: auto;
}

.status-indicator {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    margin-left: auto;
}

.status-indicator.online {
    background: #3ba55d;
    box-shadow: 0 0 8px rgba(59, 165, 93, 0.5);
}

/* Login Button */
.login-btn {
    display: flex;
    align-items: center;
    gap: 8px;
    padding: 12px 20px;
    background: linear-gradient(135deg, #5865f2 0%, #3b82f6 100%);
    color: #ffffff;
    text-decoration: none;
    border-radius: 8px;
    font-weight: 600;
    transition: all 0.3s ease;
}

.login-btn:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 32px rgba(88, 101, 242, 0.3);
}

/* Mobile Navigation */
.nav-mobile {
    display: none;
}

.mobile-actions {
    display: flex;
    align-items: center;
    gap: 16px;
}

.lang-btn-mobile {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 40px;
    height: 40px;
    background: rgba(30, 36, 57, 0.8);
    border: 1px solid rgba(44, 50, 68, 0.5);
    border-radius: 8px;
    color: #b9bbbe;
    cursor: pointer;
    transition: all 0.3s ease;
}

.lang-btn-mobile:hover {
    background: rgba(88, 101, 242, 0.1);
    color: #ffffff;
}

.mobile-menu-toggle {
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    width: 30px;
    height: 20px;
    background: transparent;
    border: none;
    cursor: pointer;
}

.mobile-menu-toggle span {
    width: 100%;
    height: 2px;
    background: #ffffff;
    border-radius: 1px;
    transition: all 0.3s ease;
}

.mobile-menu-toggle.active span:nth-child(1) {
    transform: rotate(45deg) translate(5px, 5px);
}

.mobile-menu-toggle.active span:nth-child(2) {
    opacity: 0;
}

.mobile-menu-toggle.active span:nth-child(3) {
    transform: rotate(-45deg) translate(7px, -6px);
}

.mobile-menu {
    position: absolute;
    top: 100%;
    left: 0;
    right: 0;
    background: rgba(26, 31, 53, 0.98);
    backdrop-filter: blur(20px);
    border-bottom: 1px solid rgba(44, 50, 68, 0.5);
    opacity: 0;
    visibility: hidden;
    transform: translateY(-10px);
    transition: all 0.3s ease;
}

.mobile-menu.active {
    opacity: 1;
    visibility: visible;
    transform: translateY(0);
}

.mobile-menu-content {
    padding: 24px;
}

.mobile-profile {
    display: flex;
    align-items: center;
    gap: 16px;
    margin-bottom: 24px;
}

.mobile-avatar {
    width: 48px;
    height: 48px;
    border-radius: 50%;
    border: 2px solid rgba(88, 101, 242, 0.3);
}

.mobile-user-info {
    display: flex;
    flex-direction: column;
}

.mobile-username {
    font-size: 16px;
    font-weight: 600;
    color: #ffffff;
    line-height: 1.2;
}

.mobile-discriminator {
    font-size: 14px;
    color: #72767d;
    line-height: 1.2;
}

.mobile-divider {
    height: 1px;
    background: rgba(44, 50, 68, 0.5);
    margin: 16px 0;
}

.mobile-nav-links {
    display: flex;
    flex-direction: column;
    gap: 8px;
}

.mobile-nav-link {
    display: flex;
    align-items: center;
    gap: 16px;
    padding: 16px;
    border-radius: 12px;
    text-decoration: none;
    color: #b9bbbe;
    font-size: 16px;
    font-weight: 500;
    transition: all 0.3s ease;
}

.mobile-nav-link:hover {
    background: rgba(88, 101, 242, 0.1);
    color: #ffffff;
}

.mobile-nav-link.premium {
    color: #faa61a;
}

.mobile-nav-link.logout {
    color: #ed4245;
}

.mobile-nav-link.login {
    background: linear-gradient(135deg, #5865f2 0%, #3b82f6 100%);
    color: #ffffff;
}

.mobile-nav-link i {
    width: 20px;
    text-align: center;
}

/* Responsive Design */
@media (max-width: 1024px) {
    .nav-container {
        padding: 0 16px;
    }
    
    .nav-desktop {
        gap: 20px;
    }
    
    .nav-links {
        gap: 16px;
    }
}

@media (max-width: 768px) {
    .nav-desktop {
        display: none;
    }
    
    .nav-mobile {
        display: block;
    }
    
    .nav-container {
        height: 60px;
    }
    
    .brand-logo {
        width: 36px;
        height: 36px;
    }
    
    .brand-text {
        font-size: 1.25rem;
    }
}

/* Animations */
@keyframes fadeIn {
    from {
        opacity: 0;
        transform: translateY(-10px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

.dropdown-item {
    animation: fadeIn 0.3s ease forwards;
}

.dropdown-item:nth-child(1) { animation-delay: 0.05s; }
.dropdown-item:nth-child(2) { animation-delay: 0.1s; }
.dropdown-item:nth-child(3) { animation-delay: 0.15s; }
.dropdown-item:nth-child(4) { animation-delay: 0.2s; }
.dropdown-item:nth-child(5) { animation-delay: 0.25s; }
</style>

<script>
document.addEventListener('DOMContentLoaded', function() {
    // Profile dropdown functionality
    const profileBtn = document.getElementById('profileBtn');
    const profileDropdown = document.getElementById('profileDropdown');
    
    if (profileBtn && profileDropdown) {
        profileBtn.addEventListener('click', function(e) {
            e.stopPropagation();
            profileDropdown.classList.toggle('active');
            profileBtn.classList.toggle('active');
        });
    }
    
    // Language dropdown functionality
    const langBtn = document.getElementById('langBtn');
    const langDropdown = document.getElementById('langDropdown');
    
    if (langBtn && langDropdown) {
        langBtn.addEventListener('click', function(e) {
            e.stopPropagation();
            langDropdown.classList.toggle('active');
        });
    }
    
    // Mobile menu functionality
    const mobileMenuToggle = document.getElementById('mobileMenuToggle');
    const mobileMenu = document.getElementById('mobileMenu');
    
    if (mobileMenuToggle && mobileMenu) {
        mobileMenuToggle.addEventListener('click', function() {
            mobileMenu.classList.toggle('active');
            mobileMenuToggle.classList.toggle('active');
        });
    }
    
    // Close dropdowns when clicking outside
    document.addEventListener('click', function(e) {
        if (profileDropdown && !profileBtn.contains(e.target)) {
            profileDropdown.classList.remove('active');
            profileBtn.classList.remove('active');
        }
        
        if (langDropdown && !langBtn.contains(e.target)) {
            langDropdown.classList.remove('active');
        }
        
        if (mobileMenu && !document.querySelector('.nav-mobile').contains(e.target)) {
            mobileMenu.classList.remove('active');
            mobileMenuToggle.classList.remove('active');
        }
    });
    
    // Language switching functionality
    const langOptions = document.querySelectorAll('.lang-option');
    langOptions.forEach(option => {
        option.addEventListener('click', function(e) {
            e.preventDefault();
            const selectedLang = this.dataset.lang;
            
            // Store language preference
            localStorage.setItem('preferredLanguage', selectedLang);
            
            // Apply language changes
            if (selectedLang === 'ar') {
                document.body.dir = 'rtl';
                document.documentElement.lang = 'ar';
            } else {
                document.body.dir = 'ltr';
                document.documentElement.lang = 'en';
            }
            
            // Close dropdown
            langDropdown.classList.remove('active');
            
            // You can add more language switching logic here
            console.log('Language switched to:', selectedLang);
        });
    });
    
    // Load saved language preference
    const savedLang = localStorage.getItem('preferredLanguage');
    if (savedLang === 'ar') {
        document.body.dir = 'rtl';
        document.documentElement.lang = 'ar';
    }
    
    // Mobile language button
    const langBtnMobile = document.getElementById('langBtnMobile');
    if (langBtnMobile) {
        langBtnMobile.addEventListener('click', function() {
            const currentLang = localStorage.getItem('preferredLanguage') || 'en';
            const newLang = currentLang === 'en' ? 'ar' : 'en';
            
            localStorage.setItem('preferredLanguage', newLang);
            
            if (newLang === 'ar') {
                document.body.dir = 'rtl';
                document.documentElement.lang = 'ar';
            } else {
                document.body.dir = 'ltr';
                document.documentElement.lang = 'en';
            }
        });
    }
});
</script>
