<%- include("partials/head", {bot, user, path, description, title: "Cbot - الصفحة الرئيسية" , url, image }) %>
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
<link rel="stylesheet" href="<%= domain %>/css/index.css">
<link rel="stylesheet" href="<%= domain %>/css/style.css">

<div class="page-content">
    <input style="display: none" type="text" value="<%=  bot.guilds.cache.reduce((a, g) => a + g.memberCount, 0) %>" id="size">
    <input style="display: none" type="text" value="<%= bot.guilds.cache.size %>" id="size2">

    <body>
        <!-- Navigation -->
        <%- include("partials/nav", {bot, user, path, domain}) %>

        <!-- Hero Section -->
        <section class="hero">
            <div class="hero-content">
                <!-- Logo Section -->
                <div class="hero-logo-container">
                    <img src="<%= domain %>/logo.png" alt="Cbot Logo" class="hero-logo animate-glow">
                    <div class="logo-glow-effect"></div>
                </div>
                
                <!-- Title Section -->
                <h1 class="hero-title animate-fade-in">
                    <span class="title-main">CBOT</span>
                    <span class="title-subtitle">Discord Bot</span>
                </h1>
                
                <h2 class="hero-subtitle animate-fade-in" style="animation-delay: 0.2s;">
                    <span data-lang-key="hero-subtitle">أفضل بوت Discord متعدد الأغراض</span>
                </h2>
                
                <p class="hero-description animate-fade-in" style="animation-delay: 0.4s;">
                    <span data-lang-key="hero-description">بوت قوي ومتطور لإدارة وتطوير خادم Discord الخاص بك بأحدث الميزات والأدوات المتقدمة</span>
                </p>
                
                <!-- Stats Section -->
                <div class="stats animate-fade-in" style="animation-delay: 0.6s;">
                    <div class="stat-item">
                        <span class="stat-number odometer" id="odometer"><%=  bot.guilds.cache.reduce((a, g) => a + g.memberCount, 0) %></span>
                        <span class="stat-label" data-lang-key="members">عضو</span>
                        <div class="stat-icon">
                            <i class="fas fa-users"></i>
                        </div>
                    </div>
                    <div class="stat-item">
                        <span class="stat-number odometer" id="odometer2"><%= bot.guilds.cache.size %></span>
                        <span class="stat-label" data-lang-key="servers">خادم</span>
                        <div class="stat-icon">
                            <i class="fas fa-server"></i>
                        </div>
                    </div>
                    <div class="stat-item">
                        <span class="stat-number">24/7</span>
                        <span class="stat-label" data-lang-key="uptime">متاح</span>
                        <div class="stat-icon">
                            <i class="fas fa-clock"></i>
                        </div>
                    </div>
                </div>
                
                <!-- Main Action Buttons -->
                <div class="main-buttons animate-fade-in" style="animation-delay: 0.8s;">
                    <% if (user) { %>
                    <a href="<%=domain%>/dashboard" class="btn btn-primary btn-xl">
                        <i class="fas fa-tachometer-alt"></i>
                        <span data-lang-key="dashboard">لوحة التحكم</span>
                        <div class="btn-glow"></div>
                    </a>
                    <% } else { %>
                    <a href="<%=domain%>/login" class="btn btn-primary btn-xl">
                        <i class="fab fa-discord"></i>
                        <span data-lang-key="login">تسجيل الدخول</span>
                        <div class="btn-glow"></div>
                    </a>
                    <% } %>
                    
                    <a href="<%=domain%>/invite" class="btn btn-secondary btn-xl">
                        <i class="fas fa-plus-circle"></i>
                        <span data-lang-key="add-to-discord">إضافة إلى Discord</span>
                    </a>
                </div>
                
                <!-- Navigation Buttons -->
                <div class="nav-buttons animate-fade-in" style="animation-delay: 1s;">
                    <a href="<%=domain%>/premium" class="nav-btn premium-nav">
                        <i class="fas fa-crown"></i>
                        <span data-lang-key="premium">البريميوم</span>
                    </a>
                    <a href="<%=domain%>/stats" class="nav-btn">
                        <i class="fas fa-chart-line"></i>
                        <span data-lang-key="bot-status">حالة البوت</span>
                    </a>
                    <a href="<%=domain%>/commands" class="nav-btn">
                        <i class="fas fa-terminal"></i>
                        <span data-lang-key="commands">الأوامر</span>
                    </a>
                    <a href="<%=domain%>/support" class="nav-btn">
                        <i class="fas fa-headset"></i>
                        <span data-lang-key="support">الدعم</span>
                    </a>
                </div>
            </div>
            
            <!-- Scroll Indicator -->
            <div class="scroll-indicator animate-fade-in" style="animation-delay: 1.2s;">
                <div class="scroll-text" data-lang-key="scroll-down">تصفح للأسفل</div>
                <i class="fas fa-chevron-down"></i>
            </div>
            
            <!-- Background Effects -->
            <div class="hero-particles"></div>
            <div class="hero-grid"></div>
        </section>

        <!-- Features Section -->
        <section class="features" id="features">
            <div class="features-container">
                <h2 class="features-title animate-fade-in">
                    <span data-lang-key="features-title">الميزات الرئيسية</span>
                </h2>
                
                <div class="features-grid">
                    <div class="feature-card animate-fade-in card-glow" style="animation-delay: 0.2s;">
                        <div class="feature-icon">
                            <i class="fas fa-clipboard-list"></i>
                        </div>
                        <img src="<%= domain %>/cards/logging.png" alt="Advanced Logging" class="feature-image">
                        <h3 class="feature-title" data-lang-key="logging-title">تسجيل متقدم</h3>
                        <p class="feature-description" data-lang-key="logging-desc">
                            مع Cbot يمكنك تخصيص التسجيل بالكامل. يتيح لك Cbot تعيين إعدادات السجل الخاصة بك من تسجيل الأعضاء إلى تسجيل الأوامر والصوت والمزيد!
                        </p>
                        <div class="feature-badge">
                            <span>متقدم</span>
                        </div>
                    </div>
                    
                    <div class="feature-card animate-fade-in card-glow" style="animation-delay: 0.4s;">
                        <div class="feature-icon">
                            <i class="fas fa-code"></i>
                        </div>
                        <img src="<%= domain %>/cards/embeds.png" alt="Design Embeds" class="feature-image">
                        <h3 class="feature-title" data-lang-key="embeds-title">تصميم الرسائل المدمجة</h3>
                        <p class="feature-description" data-lang-key="embeds-desc">
                            يتيح لك Cbot إنشاء وإرسال الرسائل المدمجة إلى أي قناة. يمكنك حتى إرسالها كـ webhook خاص بك أو حفظها كأمر مخصص!
                        </p>
                        <div class="feature-badge">
                            <span>إبداعي</span>
                        </div>
                    </div>
                    
                    <div class="feature-card animate-fade-in card-glow" style="animation-delay: 0.6s;">
                        <div class="feature-icon">
                            <i class="fas fa-file-alt"></i>
                        </div>
                        <img src="<%= domain %>/cards/application.png" alt="Create Applications" class="feature-image">
                        <h3 class="feature-title" data-lang-key="applications-title">إنشاء التطبيقات</h3>
                        <p class="feature-description" data-lang-key="applications-desc">
                            إدارة تطبيقات الخادم الخاص بك مع Cbot! إنشاء نماذج تطبيق مخصصة وإدارة الطلبات بسهولة.
                        </p>
                        <div class="feature-badge">
                            <span>إدارة</span>
                        </div>
                    </div>
                    
                    <div class="feature-card animate-fade-in card-glow" style="animation-delay: 0.8s;">
                        <div class="feature-icon">
                            <i class="fas fa-mouse-pointer"></i>
                        </div>
                        <img src="<%= domain %>/cards/rr.png" alt="Reaction Roles" class="feature-image">
                        <h3 class="feature-title" data-lang-key="reaction-roles-title">أدوار التفاعل</h3>
                        <p class="feature-description" data-lang-key="reaction-roles-desc">
                            إنشاء أدوار التفاعل الخاصة بك مباشرة من لوحة تحكم Cbot! سهولة في الإعداد والإدارة.
                        </p>
                        <div class="feature-badge">
                            <span>تفاعلي</span>
                        </div>
                    </div>
                </div>
                
                <!-- Additional Features -->
                <div class="extra-features animate-fade-in" style="animation-delay: 1s;">
                    <h3 class="extra-title" data-lang-key="more-features">المزيد من الميزات</h3>
                    <div class="features-list">
                        <div class="feature-item">
                            <i class="fas fa-shield-alt"></i>
                            <span data-lang-key="auto-moderation">الإشراف التلقائي</span>
                        </div>
                        <div class="feature-item">
                            <i class="fas fa-music"></i>
                            <span data-lang-key="music-player">مشغل الموسيقى</span>
                        </div>
                        <div class="feature-item">
                            <i class="fas fa-level-up-alt"></i>
                            <span data-lang-key="leveling-system">نظام المستويات</span>
                        </div>
                        <div class="feature-item">
                            <i class="fas fa-ticket-alt"></i>
                            <span data-lang-key="ticket-system">نظام التذاكر</span>
                        </div>
                        <div class="feature-item">
                            <i class="fas fa-gamepad"></i>
                            <span data-lang-key="fun-commands">أوامر التسلية</span>
                        </div>
                        <div class="feature-item">
                            <i class="fas fa-cog"></i>
                            <span data-lang-key="custom-commands">الأوامر المخصصة</span>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <!-- Call to Action Section -->
        <section class="cta-section">
            <div class="cta-container">
                <div class="cta-content animate-fade-in">
                    <h2 class="cta-title" data-lang-key="cta-title">ابدأ مع Cbot اليوم</h2>
                    <p class="cta-description" data-lang-key="cta-description">
                        انضم إلى آلاف الخوادم التي تثق في Cbot لإدارة مجتمعاتها
                    </p>
                    <div class="cta-buttons">
                        <a href="<%=domain%>/invite" class="btn btn-primary btn-xl">
                            <i class="fas fa-rocket"></i>
                            <span data-lang-key="get-started">ابدأ الآن مجاناً</span>
                        </a>
                        <a href="<%=domain%>/premium" class="btn btn-outline btn-xl">
                            <i class="fas fa-crown"></i>
                            <span data-lang-key="view-premium">عرض البريميوم</span>
                        </a>
                    </div>
                </div>
                <div class="cta-stats">
                    <div class="cta-stat">
                        <span class="cta-stat-number">99%</span>
                        <span class="cta-stat-label" data-lang-key="uptime-stat">وقت التشغيل</span>
                    </div>
                    <div class="cta-stat">
                        <span class="cta-stat-number">50+</span>
                        <span class="cta-stat-label" data-lang-key="features-stat">ميزة</span>
                    </div>
                    <div class="cta-stat">
                        <span class="cta-stat-number">24/7</span>
                        <span class="cta-stat-label" data-lang-key="support-stat">دعم فني</span>
                    </div>
                </div>
            </div>
        </section>

        <!-- Footer -->
        <%- include("partials/mainfooter", {domain, bot}) %>

    </body>
</div>

<!-- Language System -->
<script>
// Language Data
const translations = {
    ar: {
        'hero-subtitle': 'أفضل بوت Discord متعدد الأغراض',
        'hero-description': 'بوت قوي ومتطور لإدارة وتطوير خادم Discord الخاص بك بأحدث الميزات والأدوات المتقدمة',
        'members': 'عضو',
        'servers': 'خادم',
        'uptime': 'متاح',
        'dashboard': 'لوحة التحكم',
        'login': 'تسجيل الدخول',
        'add-to-discord': 'إضافة إلى Discord',
        'premium': 'البريميوم',
        'bot-status': 'حالة البوت',
        'commands': 'الأوامر',
        'support': 'الدعم',
        'scroll-down': 'تصفح للأسفل',
        'features-title': 'الميزات الرئيسية',
        'logging-title': 'تسجيل متقدم',
        'logging-desc': 'مع Cbot يمكنك تخصيص التسجيل بالكامل. يتيح لك Cbot تعيين إعدادات السجل الخاصة بك من تسجيل الأعضاء إلى تسجيل الأوامر والصوت والمزيد!',
        'embeds-title': 'تصميم الرسائل المدمجة',
        'embeds-desc': 'يتيح لك Cbot إنشاء وإرسال الرسائل المدمجة إلى أي قناة. يمكنك حتى إرسالها كـ webhook خاص بك أو حفظها كأمر مخصص!',
        'applications-title': 'إنشاء التطبيقات',
        'applications-desc': 'إدارة تطبيقات الخادم الخاص بك مع Cbot! إنشاء نماذج تطبيق مخصصة وإدارة الطلبات بسهولة.',
        'reaction-roles-title': 'أدوار التفاعل',
        'reaction-roles-desc': 'إنشاء أدوار التفاعل الخاصة بك مباشرة من لوحة تحكم Cbot! سهولة في الإعداد والإدارة.',
        'more-features': 'المزيد من الميزات',
        'auto-moderation': 'الإشراف التلقائي',
        'music-player': 'مشغل الموسيقى',
        'leveling-system': 'نظام المستويات',
        'ticket-system': 'نظام التذاكر',
        'fun-commands': 'أوامر التسلية',
        'custom-commands': 'الأوامر المخصصة',
        'cta-title': 'ابدأ مع Cbot اليوم',
        'cta-description': 'انضم إلى آلاف الخوادم التي تثق في Cbot لإدارة مجتمعاتها',
        'get-started': 'ابدأ الآن مجاناً',
        'view-premium': 'عرض البريميوم',
        'uptime-stat': 'وقت التشغيل',
        'features-stat': 'ميزة',
        'support-stat': 'دعم فني'
    },
    en: {
        'hero-subtitle': 'The Best Multipurpose Discord Bot',
        'hero-description': 'Powerful and advanced bot to manage and develop your Discord server with the latest features and advanced tools',
        'members': 'Members',
        'servers': 'Servers',
        'uptime': 'Online',
        'dashboard': 'Dashboard',
        'login': 'Login',
        'add-to-discord': 'Add to Discord',
        'premium': 'Premium',
        'bot-status': 'Bot Status',
        'commands': 'Commands',
        'support': 'Support',
        'scroll-down': 'Scroll Down',
        'features-title': 'Main Features',
        'logging-title': 'Advanced Logging',
        'logging-desc': 'With Cbot you can fully customize logging. Cbot lets you set your own log settings from member logging to command and voice logging and more!',
        'embeds-title': 'Design Embeds',
        'embeds-desc': 'Cbot allows you to create and send embeds to any channel. You can even send it as your own webhook or save it as a custom command!',
        'applications-title': 'Create Applications',
        'applications-desc': 'Manage your server applications with Cbot! Create custom application forms and manage requests easily.',
        'reaction-roles-title': 'Reaction Roles',
        'reaction-roles-desc': 'Create your own reaction roles directly from Cbot\'s Dashboard! Easy setup and management.',
        'more-features': 'More Features',
        'auto-moderation': 'Auto Moderation',
        'music-player': 'Music Player',
        'leveling-system': 'Leveling System',
        'ticket-system': 'Ticket System',
        'fun-commands': 'Fun Commands',
        'custom-commands': 'Custom Commands',
        'cta-title': 'Get Started with Cbot Today',
        'cta-description': 'Join thousands of servers that trust Cbot to manage their communities',
        'get-started': 'Get Started Free',
        'view-premium': 'View Premium',
        'uptime-stat': 'Uptime',
        'features-stat': 'Features',
        'support-stat': 'Support'
    }
};

let currentLanguage = 'ar';

// Initialize language system
document.addEventListener('DOMContentLoaded', function() {
    // Set initial language
    applyLanguage(currentLanguage);
    
    // Initialize animations
    initializeAnimations();
    
    // Initialize scroll effects
    initializeScrollEffects();
    
    // Initialize particles
    initializeParticles();
});

function applyLanguage(lang) {
    currentLanguage = lang;
    
    // Update all text elements
    const elements = document.querySelectorAll('[data-lang-key]');
    elements.forEach(element => {
        const key = element.getAttribute('data-lang-key');
        if (translations[lang] && translations[lang][key]) {
            element.textContent = translations[lang][key];
        }
    });
    
    // Update document direction for Arabic
    if (lang === 'ar') {
        document.documentElement.dir = 'rtl';
        document.documentElement.lang = 'ar';
    } else {
        document.documentElement.dir = 'ltr';
        document.documentElement.lang = lang;
    }
}

// Animation initialization
function initializeAnimations() {
    const observerOptions = {
        threshold: 0.1,
        rootMargin: '0px 0px -50px 0px'
    };
    
    const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                entry.target.style.opacity = '1';
                entry.target.style.transform = 'translateY(0)';
            }
        });
    }, observerOptions);
    
    // Observe all animated elements
    document.querySelectorAll('.animate-fade-in').forEach(el => {
        el.style.opacity = '0';
        el.style.transform = 'translateY(30px)';
        el.style.transition = 'opacity 0.8s ease, transform 0.8s ease';
        observer.observe(el);
    });
}

// Scroll effects
function initializeScrollEffects() {
    // Smooth scrolling for scroll indicator
    document.querySelector('.scroll-indicator').addEventListener('click', function() {
        document.querySelector('#features').scrollIntoView({
            behavior: 'smooth'
        });
    });
    
    // Parallax effect for hero
    window.addEventListener('scroll', function() {
        const scrolled = window.pageYOffset;
        const parallax = scrolled * 0.5;
        
        const hero = document.querySelector('.hero');
        if (hero) {
            hero.style.transform = `translateY(${parallax}px)`;
        }
    });
}

// Particles animation
function initializeParticles() {
    const particlesContainer = document.querySelector('.hero-particles');
    if (!particlesContainer) return;
    
    // Create floating particles
    for (let i = 0; i < 20; i++) {
        const particle = document.createElement('div');
        particle.className = 'particle';
        particle.style.cssText = `
            position: absolute;
            width: ${Math.random() * 4 + 2}px;
            height: ${Math.random() * 4 + 2}px;
            background: #FFD700;
            border-radius: 50%;
            opacity: ${Math.random() * 0.5 + 0.2};
            left: ${Math.random() * 100}%;
            top: ${Math.random() * 100}%;
            animation: float ${Math.random() * 10 + 10}s infinite ease-in-out;
            animation-delay: ${Math.random() * 5}s;
        `;
        particlesContainer.appendChild(particle);
    }
}
</script>

<!-- Additional Styles -->
<style>
/* Hero Section Enhancements */
.hero-logo-container {
    position: relative;
    margin-bottom: var(--space-2xl);
}

.logo-glow-effect {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 200px;
    height: 200px;
    background: radial-gradient(circle, rgba(255, 215, 0, 0.3) 0%, transparent 70%);
    border-radius: 50%;
    animation: pulse-glow 3s ease-in-out infinite;
}

.title-main {
    display: block;
    font-size: 1.2em;
    margin-bottom: -0.2em;
}

.title-subtitle {
    display: block;
    font-size: 0.6em;
    opacity: 0.8;
    font-weight: 400;
}

.stat-item {
    position: relative;
    overflow: hidden;
}

.stat-icon {
    position: absolute;
    top: 1rem;
    right: 1rem;
    font-size: 1.5rem;
    opacity: 0.3;
    color: var(--gold-primary);
}

.btn-glow {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(45deg, transparent, rgba(255, 215, 0, 0.2), transparent);
    transform: translateX(-100%);
    transition: transform 0.6s ease;
}

.btn:hover .btn-glow {
    transform: translateX(100%);
}

.premium-nav {
    background: linear-gradient(135deg, var(--gold-primary), var(--gold-secondary));
    color: var(--black-soft) !important;
    font-weight: 700;
}

.premium-nav:hover {
    background: linear-gradient(135deg, var(--gold-secondary), var(--gold-accent));
    box-shadow: var(--shadow-golden-glow);
}

.scroll-indicator {
    position: absolute;
    bottom: 2rem;
    left: 50%;
    transform: translateX(-50%);
    text-align: center;
    cursor: pointer;
    color: var(--gold-primary);
    transition: var(--transition-normal);
}

.scroll-indicator:hover {
    transform: translateX(-50%) translateY(-5px);
    color: var(--gold-secondary);
}

.scroll-text {
    font-size: var(--text-sm);
    margin-bottom: 0.5rem;
    font-weight: 500;
}

.scroll-indicator i {
    animation: bounce-slow 2s infinite;
}

/* Feature Card Enhancements */
.feature-icon {
    position: absolute;
    top: 1rem;
    right: 1rem;
    font-size: 2rem;
    color: var(--gold-primary);
    opacity: 0.2;
    transition: var(--transition-normal);
}

.feature-card:hover .feature-icon {
    opacity: 0.8;
    transform: scale(1.1);
}

.feature-badge {
    position: absolute;
    top: -0.5rem;
    left: 1rem;
    background: linear-gradient(45deg, var(--gold-primary), var(--gold-secondary));
    color: var(--black-soft);
    padding: 0.25rem 0.75rem;
    border-radius: var(--radius-full);
    font-size: var(--text-xs);
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.5px;
}

.extra-features {
    margin-top: var(--space-3xl);
    text-align: center;
}

.extra-title {
    color: var(--gold-secondary);
    margin-bottom: var(--space-xl);
    font-size: var(--text-2xl);
}

.features-list {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: var(--space-lg);
    max-width: 800px;
    margin: 0 auto;
}

.feature-item {
    display: flex;
    align-items: center;
    gap: var(--space-sm);
    padding: var(--space-md);
    background: rgba(255, 215, 0, 0.05);
    border-radius: var(--radius-md);
    border: 1px solid rgba(255, 215, 0, 0.1);
    transition: var(--transition-normal);
}

.feature-item:hover {
    background: rgba(255, 215, 0, 0.1);
    border-color: var(--gold-accent);
    transform: translateY(-2px);
}

.feature-item i {
    color: var(--gold-primary);
    font-size: var(--text-lg);
}

/* CTA Section */
.cta-section {
    background: linear-gradient(135deg, var(--bg-secondary) 0%, var(--bg-tertiary) 100%);
    padding: var(--space-3xl) 0;
    margin-top: var(--space-3xl);
}

.cta-container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 var(--space-lg);
    display: grid;
    grid-template-columns: 2fr 1fr;
    gap: var(--space-3xl);
    align-items: center;
}

.cta-title {
    color: var(--gold-primary);
    margin-bottom: var(--space-lg);
}

.cta-description {
    font-size: var(--text-lg);
    margin-bottom: var(--space-xl);
    color: var(--text-secondary);
}

.cta-buttons {
    display: flex;
    gap: var(--space-lg);
    flex-wrap: wrap;
}

.cta-stats {
    display: flex;
    flex-direction: column;
    gap: var(--space-lg);
}

.cta-stat {
    text-align: center;
    padding: var(--space-lg);
    background: var(--bg-card);
    border-radius: var(--radius-lg);
    border: 1px solid rgba(255, 215, 0, 0.1);
}

.cta-stat-number {
    display: block;
    font-size: var(--text-3xl);
    font-weight: 900;
    color: var(--gold-primary);
    margin-bottom: var(--space-sm);
}

.cta-stat-label {
    color: var(--text-secondary);
    font-size: var(--text-sm);
    text-transform: uppercase;
    letter-spacing: 0.5px;
}

/* Background Effects */
.hero-particles {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    pointer-events: none;
    overflow: hidden;
}

.hero-grid {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image: 
        linear-gradient(rgba(255, 215, 0, 0.03) 1px, transparent 1px),
        linear-gradient(90deg, rgba(255, 215, 0, 0.03) 1px, transparent 1px);
    background-size: 50px 50px;
    pointer-events: none;
}

/* Animations */
@keyframes pulse-glow {
    0%, 100% { transform: translate(-50%, -50%) scale(1); opacity: 0.3; }
    50% { transform: translate(-50%, -50%) scale(1.1); opacity: 0.5; }
}

@keyframes bounce-slow {
    0%, 20%, 50%, 80%, 100% { transform: translateY(0); }
    40% { transform: translateY(-10px); }
    60% { transform: translateY(-5px); }
}

@keyframes float {
    0%, 100% { transform: translateY(0px) rotate(0deg); }
    25% { transform: translateY(-20px) rotate(90deg); }
    50% { transform: translateY(-10px) rotate(180deg); }
    75% { transform: translateY(-30px) rotate(270deg); }
}

/* Responsive Design */
@media (max-width: 768px) {
    .cta-container {
        grid-template-columns: 1fr;
        text-align: center;
    }
    
    .cta-stats {
        flex-direction: row;
        justify-content: space-around;
    }
    
    .features-list {
        grid-template-columns: 1fr;
    }
    
    .cta-buttons {
        justify-content: center;
    }
}
</style>

<!-- Odometer for animated counters -->
<script src='https://github.hubspot.com/odometer/odometer.js'></script>
<link rel="stylesheet" href="https://github.hubspot.com/odometer/themes/odometer-theme-default.css">

<%- include("partials/footer") %>
